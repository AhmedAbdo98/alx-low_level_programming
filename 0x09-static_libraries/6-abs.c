#include "main.h"

/**
 * _abs - Computes the absolute val of an int
 * @n: The integer to check
 * Return: The absolute val of integer
 */
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
return (-n);
}
